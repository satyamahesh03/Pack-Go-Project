import React from "react";
import "./Loading.css"; // Import the loading animation CSS

const Loading = () => {
  return (
    <div className="loading-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Loading;